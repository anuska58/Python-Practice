# price = 5
#
# assert price >= 0, "Price must be positive"
#
# print("After assert")    # Assert is used to stop the program if the condition is false.

age = int(input("Enter your age: "))
assert age > 0, "Age should be greater than zero and must be positive"
print(age)
